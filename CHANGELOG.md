# Changelog

## [1.2.4] - 2026-07-18

### Fixed
- `execute_astrbot_command` 改为通过 `FunctionTool` 显式注册 JSON Schema，强制暴露必填的 `command` 字段，避免装饰器注册链路下 Agent 仍收到空 `properties`。

## [1.2.3] - 2026-07-18

### Fixed
- 修复 `execute_astrbot_command` 缺少 AstrBot 可解析的 `Args` 参数说明而被注册为空 schema 的问题；现在 AI 可传入 `command`，无额外参数的目标命令也应传入其完整触发式。

## [1.2.2] - 2026-07-18

### Fixed
- 修复 AI 直接调用自定义正则命令的实际触发文本（如 `nte帮助`）时，被误判为仅命中通用处理器而拒绝调度的问题。

## [1.2.1] - 2026-07-18

### Fixed
- 修复 AI 执行自定义帮助组命令时，目录中保留的命令前缀或大小写差异会被误判为仅命中通用处理器的问题。
- 自定义命令主触发式和别名现在按当前命令前缀与大小写规则识别，即使只命中通用处理器也会继续调度。

## [1.2.0] - 2026-07-14

### Added
- 新增 AI 自定义命令组管理工具，支持分组与命令的查询、新增、更新、删除；写入操作仅限管理员。
- 新增分组删除预览与一次性确认 token，避免 AI 误删整组目录。
- `execute_astrbot_command` 新增 `auto`、`background` 与 `custom` 结果监听模式；短命令可返回完整结果，长命令保持后台运行。
- 新增 AI 命令结果监听配置：`ai_command_auto_wait_seconds` 与 `ai_command_max_wait_seconds`。

### Fixed
- 自定义命令组持久化改为原子写入，写入失败不会污染内存配置。
- 帮助图片缓存键纳入完整自定义命令组内容，命令、别名、可见性和优先级变更会正确失效。
- 命令调度初始化卡住、管道停止和流式并发输出现在会返回真实状态，不再伪造成功或遗漏结果。

### Improved
- Web API 与 AI tools 复用同一自定义命令组服务，统一校验、缓存失效和错误反馈。
- 补充自定义组、配置持久化与命令结果监听的回归测试。

## [1.1.0] - 2026-05-31

### Added
- AI 命令执行异步化，避免阻塞事件循环，提升响应性能
- 新增 Executor actor 功能测试 (`test_executor_actor.py`)
- 新增 `AGENTS.md` Agent 协作入口指令
- 新增 `docs/` 文档目录，将项目文档拆分为 `docs/project/` 和 `docs/dev/`
- 新增 `MIGRATION_SUMMARY.md` 迁移说明文档

### Fixed
- 修复命令分发未发送事件的问题
- 修复 self actor 配置复制逻辑错误
- 处理代码审查反馈的遗留问题

### Improved
- 重写 `CLAUDE.md`，从英文项目概述改为精简中文入口规则
- 更新 `_conf_schema.json` 配置 schema 字段
- 使用本地 ruff pre-commit hooks 替代 pre-commit.ci
- 重命名自定义命令组页面目录

## [1.0.3] - 2026-05-15

### Fixed
- 修复自定义命令描述字段在 WebUI API 中的连接问题
- 解决代码合并冲突导致插件无法加载的语法错误

### Added
- 插件加载时自动初始化帮助索引 JSON 缓存，提升首次帮助菜单响应速度
- 图片渲染仍采用延迟加载策略（首次请求时才生成），平衡启动速度与运行时性能

### Improved
- 修复缓存键生成逻辑，将自定义命令描述纳入缓存哈希计算
- 缓存失效策略更智能：自定义命令描述变更时自动清除相关缓存

## [1.0.2] - 2026-05-15

### Fixed
- 修复 `enable_ai_command_notify` 和 `enable_ai_command_result` 配置项不生效的问题

### Added
- 自定义命令现在支持命令描述字段，可在帮助菜单中显示命令说明（类似真实插件命令）
- WebUI 自定义命令组表单新增"命令描述"输入框，支持为普通命令和正则命令添加描述

### Improved
- AI 命令执行现在能够自动识别并正确处理普通命令和正则命令的差异
- 搜索命令结果中新增显示当前系统配置的命令前缀列表（如 /、!、#）
- 现在支持使用非标准前缀（如 ! 或 #）的命令系统，AI 会自动适配

## [1.0.1] - 2026-05-15

### Fixed
- 为 pages 页面加载前增加一层遮盖层使其显示更自然
